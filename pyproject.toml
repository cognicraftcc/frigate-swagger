[project]
name = "frigate-swagger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.1",
    "flask-swagger-ui>=5.21.0",
    "gunicorn>=23.0.0",
    "pyyaml>=6.0.2",
]
